// Package modelproviders is the abstraction over LLM HTTP APIs (Anthropic,
// OpenAI, Mistral, Ollama). All providers consume the same CallRequest /
// CallResponse value types so the agent runtime is provider-agnostic.
package modelproviders
